#!/bin/bash
# Display psql command line to connect to running postgres

set -e
. etc/env
#This isn't used anymore, either.
export POSTGRES_CLI="psql -p $POSTGRES_PORT -U root -h $POSTGRES_HOST -d postgres"
